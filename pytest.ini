[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
addopts = -ra --strict-markers --cov=app --cov-config=.coveragerc --cov-report=term-missing
markers =
    edge_case: parametrized input/output edge-case tests for documentation tables
